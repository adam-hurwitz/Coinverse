<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="app.carpecoin.contentFeed.models.Content" />

        <variable
            name="viewmodel"
            type="app.carpecoin.contentFeed.ContentViewModel" />

    </data>

    <androidx.cardview.widget.CardView
        style="@style/contentCardStyle"
        android:onClick="@{() -> viewmodel.contentClicked(data)}"
        app:cardCornerRadius="0dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/cell_content_feed"
            style="@style/contentConstraintStyle">

            <TextView
                android:id="@+id/creator"
                style="@style/contentCreatorStyle"
                android:text='@{data.creator, default="creator name"}'
                app:layout_constraintBottom_toTopOf="@+id/preview"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/timeAgo"
                style="@style/timeAgoStyle"
                app:layout_constraintBottom_toBottomOf="@id/creator"
                app:layout_constraintLeft_toRightOf="@+id/creator"
                app:timePostedAgo="@{data.timestamp.time}"
                tools:text="time ago posted" />

            <ImageView
                android:id="@+id/preview"
                style="@style/contentPreviewImageStyle"
                app:imageUrl="@{data.previewImage}"
                app:layout_constraintBottom_toTopOf="@id/title"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/creator" />

            <TextView
                android:id="@+id/title"
                style="@style/contentTitleStyle"
                android:text='@{data.contentTitle, default="content title"}'
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toBottomOf="@id/preview" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>